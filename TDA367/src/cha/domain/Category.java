package cha.domain;

	/**
	 * @author Malla
	 *The class enum Category keeps track of all different categories available
	 */
	public enum Category{
		BODYTOBODY,
		BACKWARDS,
		SAMECLASS,
		WORDJUMBLE
}
